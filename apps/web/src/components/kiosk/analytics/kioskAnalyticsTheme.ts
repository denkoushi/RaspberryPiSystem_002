/**
 * キオスク集計パネル共通のテーマ（DADS トークン参照）。
 * コンポーネント間で見た目を揃えるための値オブジェクト。
 */
export type KioskAnalyticsTheme = {
  chartBorrow: string;
  chartReturn: string;
  strokeBar: string;
  surface: string;
  border: string;
  borderSubtle: string;
  text: string;
  textMuted: string;
  textSub: string;
  primaryUi: string;
  tabInactive: string;
  error: string;
  radius8: string;
  radius6: string;
};
