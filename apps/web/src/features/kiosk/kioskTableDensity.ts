/** Compact structural grammar shared by kiosk table row actions. */
export const kioskDenseTableRowActionStructureClassName =
  'inline-flex !h-[30.8px] !min-h-[30.8px] min-w-[1.5rem] shrink-0 items-center justify-center rounded !px-1 !py-0 leading-none whitespace-nowrap';

/** Allows actions to stay horizontal and wrap only when the cell is genuinely narrow. */
export const kioskDenseTableRowActionsClassName =
  'flex min-w-0 flex-wrap items-center justify-end gap-1';
