/** パレット番号グリッドの列数 */
export const PALLET_GRID_COLS = 5;

/**
 * サーバが `palletCount` を返す前のフォールバック（既定 10）。
 * @deprecated 可能な限り `currentMachine.palletCount` を使う
 */
export const DEFAULT_KIOSK_PALLET_COUNT = 10;
