---
description: 旧ルール互換メモ（Git安全運用は新ファイルへ移行済み）。⚠️ 閉架: 通常は参照不要。
alwaysApply: false
---

# git-workflow（Deprecated / 閉架 / アーカイブ）

⚠️ **閉架扱い: 通常は参照しないでください。** このファイルは互換のために残していますが、内容は新しい汎用ルールへ移行済みです。

**移動元**: `.cursor/rules/git-workflow.mdc`（2025-01-XXに`docs/archive/rules/`へ移動）

- 参照先:
  - `.cursor/rules/20-git-workflow.mdc`
