services:
  api:
    # This override is used only with `docker compose run --rm --no-deps`.
    # The ordinary API service never loads migration authority.
    env_file:
      - ${MIGRATION_DATABASE_ENV_FILE:?MIGRATION_DATABASE_ENV_FILE is required}
