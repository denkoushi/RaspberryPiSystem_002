{
  auto_https off
}
:80 {
  root * /srv/site

  handle_path /api/* {
    reverse_proxy api:8080
  }

  handle_path /ws/* {
    reverse_proxy api:8080
  }

  @spa {
    not path /api/*
    not path /ws/*
    not file
  }
  rewrite @spa /index.html
  file_server
}
