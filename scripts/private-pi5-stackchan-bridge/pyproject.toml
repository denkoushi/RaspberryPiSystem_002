[project]
name = "stackchan-private-pi5-bridge"
version = "0.1.0"
description = "Private Pi5 bridge for StackChan to DGX runtime"
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[project.optional-dependencies]
local-stt = [
  "faster-whisper>=1.1.0",
]

[tool.uv]
package = false
