diff --git a/firmware/src/llm/ChatGPT/ChatGPT.cpp b/firmware/src/llm/ChatGPT/ChatGPT.cpp
index d378e22..4bc26f4 100644
--- a/firmware/src/llm/ChatGPT/ChatGPT.cpp
+++ b/firmware/src/llm/ChatGPT/ChatGPT.cpp
@@ -3,6 +3,7 @@
 #include <SPIFFS.h>
 #include <Avatar.h>
 #include <HTTPClient.h>
+#include <WiFiClient.h>
 #include <WiFiClientSecure.h>
 #include "rootCA/rootCACertificate.h"
 #include <ArduinoJson.h>
@@ -16,6 +17,22 @@
 using namespace m5avatar;
 extern Avatar avatar;
 
+#ifndef CHATGPT_API_URL
+#define CHATGPT_API_URL "https://api.openai.com/v1/chat/completions"
+#endif
+
+#ifndef CHATGPT_API_USE_AUTH_BEARER
+#define CHATGPT_API_USE_AUTH_BEARER 1
+#endif
+
+#ifndef CHATGPT_STACKCHAN_TOKEN
+#define CHATGPT_STACKCHAN_TOKEN ""
+#endif
+
+#ifndef CHATGPT_HTTP_MAX_ATTEMPTS
+#define CHATGPT_HTTP_MAX_ATTEMPTS 2
+#endif
+
 
 const String json_ChatString = 
 "{\"model\": \"gpt-4o\","
@@ -145,46 +162,98 @@ void ChatGPT::load_role(){
 
 String ChatGPT::https_post_json(const char* url, const char* json_string, const char* root_ca) {
   String payload = "";
-  WiFiClientSecure *client = new WiFiClientSecure;
-  if(client) {
-    client -> setCACert(root_ca);
-    {
-      // Add a scoping block for HTTPClient https to make sure it is destroyed before WiFiClientSecure *client is 
-      HTTPClient https;
-      https.setTimeout( 65000 ); 
-  
-      Serial.print("[HTTPS] begin...\n");
-      if (https.begin(*client, url)) {  // HTTPS
-        Serial.print("[HTTPS] POST...\n");
-        // start connection and send HTTP header
-        https.addHeader("Content-Type", "application/json");
-        https.addHeader("Authorization", String("Bearer ") + param.api_key);
-        int httpCode = https.POST((uint8_t *)json_string, strlen(json_string));
-  
-        // httpCode will be negative on error
-        if (httpCode > 0) {
-          // HTTP header has been send and Server response header has been handled
-          Serial.printf("[HTTPS] POST... code: %d\n", httpCode);
-  
-          // file found at server
-          if (httpCode == HTTP_CODE_OK || httpCode == HTTP_CODE_MOVED_PERMANENTLY) {
-            payload = https.getString();
+  const bool isHttps = String(url).startsWith("https://");
+  for (int attempt = 1; attempt <= CHATGPT_HTTP_MAX_ATTEMPTS; attempt++) {
+    HTTPClient http;
+    http.setTimeout(65000);
+    int httpCode = -1;
+
+    if (isHttps) {
+      WiFiClientSecure secureClient;
+      secureClient.setCACert(root_ca);
+      Serial.printf("[HTTPS] begin... %s (attempt=%d/%d)\n", url, attempt, CHATGPT_HTTP_MAX_ATTEMPTS);
+      if (http.begin(secureClient, url)) {
+        http.addHeader("Content-Type", "application/json");
+        if (strlen(CHATGPT_STACKCHAN_TOKEN) > 0) {
+          http.addHeader("X-Stackchan-Token", CHATGPT_STACKCHAN_TOKEN);
+        }
+#if CHATGPT_API_USE_AUTH_BEARER
+        if (param.api_key.length() > 0) {
+          http.addHeader("Authorization", String("Bearer ") + param.api_key);
+        }
+#endif
+        httpCode = http.POST((uint8_t *)json_string, strlen(json_string));
+      }
+      if (httpCode > 0) {
+        Serial.printf("[HTTP] POST... code: %d (attempt=%d/%d)\n", httpCode, attempt, CHATGPT_HTTP_MAX_ATTEMPTS);
+        payload = http.getString();
+        Serial.printf("[HTTP] payload length: %d\n", payload.length());
+        if (httpCode == HTTP_CODE_OK || httpCode == HTTP_CODE_MOVED_PERMANENTLY) {
+          if (payload != "") {
             Serial.println("//////////////");
             Serial.println(payload);
             Serial.println("//////////////");
+            http.end();
+            return payload;
           }
-        } else {
-          Serial.printf("[HTTPS] POST... failed, error: %s\n", https.errorToString(httpCode).c_str());
-        }  
-        https.end();
+          Serial.println("[HTTP] empty payload; retrying");
+        } else if (attempt == CHATGPT_HTTP_MAX_ATTEMPTS) {
+          http.end();
+          return payload;
+        }
       } else {
-        Serial.printf("[HTTPS] Unable to connect\n");
+        String errorMessage = http.errorToString(httpCode);
+        Serial.printf("[HTTP] POST... failed, error: %s (attempt=%d/%d)\n", errorMessage.c_str(), attempt, CHATGPT_HTTP_MAX_ATTEMPTS);
+        payload = String("{\"error\":{\"message\":\"http post failed: ") + errorMessage + String("\"}}");
+        if (attempt == CHATGPT_HTTP_MAX_ATTEMPTS) {
+          http.end();
+          return payload;
+        }
       }
-      // End extra scoping block
-    }  
-    delete client;
-  } else {
-    Serial.println("Unable to create client");
+      http.end();
+    } else {
+      WiFiClient plainClient;
+      Serial.printf("[HTTP] begin... %s (attempt=%d/%d)\n", url, attempt, CHATGPT_HTTP_MAX_ATTEMPTS);
+      if (http.begin(plainClient, url)) {
+        http.addHeader("Content-Type", "application/json");
+        if (strlen(CHATGPT_STACKCHAN_TOKEN) > 0) {
+          http.addHeader("X-Stackchan-Token", CHATGPT_STACKCHAN_TOKEN);
+        }
+#if CHATGPT_API_USE_AUTH_BEARER
+        if (param.api_key.length() > 0) {
+          http.addHeader("Authorization", String("Bearer ") + param.api_key);
+        }
+#endif
+        httpCode = http.POST((uint8_t *)json_string, strlen(json_string));
+      }
+      if (httpCode > 0) {
+        Serial.printf("[HTTP] POST... code: %d (attempt=%d/%d)\n", httpCode, attempt, CHATGPT_HTTP_MAX_ATTEMPTS);
+        payload = http.getString();
+        Serial.printf("[HTTP] payload length: %d\n", payload.length());
+        if (httpCode == HTTP_CODE_OK || httpCode == HTTP_CODE_MOVED_PERMANENTLY) {
+          if (payload != "") {
+            Serial.println("//////////////");
+            Serial.println(payload);
+            Serial.println("//////////////");
+            http.end();
+            return payload;
+          }
+          Serial.println("[HTTP] empty payload; retrying");
+        } else if (attempt == CHATGPT_HTTP_MAX_ATTEMPTS) {
+          http.end();
+          return payload;
+        }
+      } else {
+        String errorMessage = http.errorToString(httpCode);
+        Serial.printf("[HTTP] POST... failed, error: %s (attempt=%d/%d)\n", errorMessage.c_str(), attempt, CHATGPT_HTTP_MAX_ATTEMPTS);
+        payload = String("{\"error\":{\"message\":\"http post failed: ") + errorMessage + String("\"}}");
+        if (attempt == CHATGPT_HTTP_MAX_ATTEMPTS) {
+          http.end();
+          return payload;
+        }
+      }
+      http.end();
+    }
   }
   return payload;
 }
@@ -290,7 +359,7 @@ String ChatGPT::execChatGpt(String json_string, String& calledFunc) {
   avatar.setExpression(Expression::Doubt);
   avatar.setSpeechFont(&fonts::efontJA_16);
   avatar.setSpeechText("考え中…");
-  String ret = https_post_json("https://api.openai.com/v1/chat/completions", json_string.c_str(), root_ca_openai);
+  String ret = https_post_json(CHATGPT_API_URL, json_string.c_str(), root_ca_openai);
   avatar.setExpression(Expression::Neutral);
   avatar.setSpeechText("");
   Serial.println(ret);
@@ -307,6 +376,27 @@ String ChatGPT::execChatGpt(String json_string, String& calledFunc) {
       avatar.setSpeechText("");
       avatar.setExpression(Expression::Neutral);
     }else{
+      const char* simple_reply = doc["replyText"];
+      if(simple_reply != 0){
+        Serial.println(simple_reply);
+        response = String(simple_reply);
+        std::replace(response.begin(),response.end(),'\n',' ');
+        calledFunc = String("");
+        return response;
+      }
+
+      const char* error_message = doc["error"]["message"];
+      if(error_message != 0){
+        Serial.println(error_message);
+        avatar.setExpression(Expression::Sad);
+        avatar.setSpeechText("エラーです");
+        response = String(error_message);
+        delay(1000);
+        avatar.setSpeechText("");
+        avatar.setExpression(Expression::Neutral);
+        return response;
+      }
+
       const char* data = doc["choices"][0]["message"]["content"];
       
       // content = nullならfunction call
diff --git a/firmware/platformio.ini b/firmware/platformio.ini
index 1f7b0ff..8c9e413 100644
--- a/firmware/platformio.ini
+++ b/firmware/platformio.ini
@@ -129,7 +129,7 @@ monitor_speed = 115200
 upload_speed = 1500000
 lib_deps = 
-	m5stack/M5Unified @ 0.1.17
+	m5stack/M5Unified @ 0.2.7
 	earlephilhower/ESP8266Audio @ ^1.9.7
 	bblanchon/ArduinoJson @ ^7
 	ESP32WebServer
diff --git a/firmware/src/llm/ChatGPT/ChatGPT.cpp b/firmware/src/llm/ChatGPT/ChatGPT.cpp
index d378e22..75bcca1 100644
--- a/firmware/src/llm/ChatGPT/ChatGPT.cpp
+++ b/firmware/src/llm/ChatGPT/ChatGPT.cpp
@@ -3,6 +3,7 @@
 #include <SPIFFS.h>
 #include <Avatar.h>
 #include <HTTPClient.h>
+#include <WiFiClient.h>
 #include <WiFiClientSecure.h>
 #include "rootCA/rootCACertificate.h"
 #include <ArduinoJson.h>
@@ -16,6 +17,22 @@
 using namespace m5avatar;
 extern Avatar avatar;
 
+#ifndef CHATGPT_API_URL
+#define CHATGPT_API_URL "https://api.openai.com/v1/chat/completions"
+#endif
+
+#ifndef CHATGPT_API_USE_AUTH_BEARER
+#define CHATGPT_API_USE_AUTH_BEARER 1
+#endif
+
+#ifndef CHATGPT_STACKCHAN_TOKEN
+#define CHATGPT_STACKCHAN_TOKEN ""
+#endif
+
+#ifndef CHATGPT_HTTP_MAX_ATTEMPTS
+#define CHATGPT_HTTP_MAX_ATTEMPTS 2
+#endif
+
 
 const String json_ChatString = 
 "{\"model\": \"gpt-4o\","
@@ -145,47 +158,72 @@ void ChatGPT::load_role(){
 
 String ChatGPT::https_post_json(const char* url, const char* json_string, const char* root_ca) {
   String payload = "";
-  WiFiClientSecure *client = new WiFiClientSecure;
-  if(client) {
-    client -> setCACert(root_ca);
-    {
-      // Add a scoping block for HTTPClient https to make sure it is destroyed before WiFiClientSecure *client is 
-      HTTPClient https;
-      https.setTimeout( 65000 ); 
-  
-      Serial.print("[HTTPS] begin...\n");
-      if (https.begin(*client, url)) {  // HTTPS
-        Serial.print("[HTTPS] POST...\n");
-        // start connection and send HTTP header
-        https.addHeader("Content-Type", "application/json");
-        https.addHeader("Authorization", String("Bearer ") + param.api_key);
-        int httpCode = https.POST((uint8_t *)json_string, strlen(json_string));
-  
-        // httpCode will be negative on error
-        if (httpCode > 0) {
-          // HTTP header has been send and Server response header has been handled
-          Serial.printf("[HTTPS] POST... code: %d\n", httpCode);
-  
-          // file found at server
-          if (httpCode == HTTP_CODE_OK || httpCode == HTTP_CODE_MOVED_PERMANENTLY) {
-            payload = https.getString();
-            Serial.println("//////////////");
-            Serial.println(payload);
-            Serial.println("//////////////");
-          }
-        } else {
-          Serial.printf("[HTTPS] POST... failed, error: %s\n", https.errorToString(httpCode).c_str());
-        }  
-        https.end();
-      } else {
-        Serial.printf("[HTTPS] Unable to connect\n");
-      }
-      // End extra scoping block
-    }  
-    delete client;
-  } else {
-    Serial.println("Unable to create client");
-  }
+  const bool isHttps = String(url).startsWith("https://");
+
+  for (int attempt = 1; attempt <= CHATGPT_HTTP_MAX_ATTEMPTS; attempt++) {
+    HTTPClient http;
+    http.setTimeout(65000);
+    int httpCode = -1;
+
+    if (isHttps) {
+      WiFiClientSecure secureClient;
+      secureClient.setCACert(root_ca);
+      Serial.printf("[HTTPS] begin... %s (attempt=%d/%d)\n", url, attempt, CHATGPT_HTTP_MAX_ATTEMPTS);
+      if (http.begin(secureClient, url)) {
+        http.addHeader("Content-Type", "application/json");
+        if (strlen(CHATGPT_STACKCHAN_TOKEN) > 0) {
+          http.addHeader("X-Stackchan-Token", CHATGPT_STACKCHAN_TOKEN);
+        }
+#if CHATGPT_API_USE_AUTH_BEARER
+        if (param.api_key.length() > 0) {
+          http.addHeader("Authorization", String("Bearer ") + param.api_key);
+        }
+#endif
+        httpCode = http.POST((uint8_t *)json_string, strlen(json_string));
+      }
+    } else {
+      WiFiClient plainClient;
+      Serial.printf("[HTTP] begin... %s (attempt=%d/%d)\n", url, attempt, CHATGPT_HTTP_MAX_ATTEMPTS);
+      if (http.begin(plainClient, url)) {
+        http.addHeader("Content-Type", "application/json");
+        if (strlen(CHATGPT_STACKCHAN_TOKEN) > 0) {
+          http.addHeader("X-Stackchan-Token", CHATGPT_STACKCHAN_TOKEN);
+        }
+#if CHATGPT_API_USE_AUTH_BEARER
+        if (param.api_key.length() > 0) {
+          http.addHeader("Authorization", String("Bearer ") + param.api_key);
+        }
+#endif
+        httpCode = http.POST((uint8_t *)json_string, strlen(json_string));
+      }
+    }
+
+    if (httpCode > 0) {
+      Serial.printf("[HTTP] POST... code: %d (attempt=%d/%d)\n", httpCode, attempt, CHATGPT_HTTP_MAX_ATTEMPTS);
+      if (httpCode == HTTP_CODE_OK || httpCode == HTTP_CODE_MOVED_PERMANENTLY) {
+        payload = http.getString();
+        Serial.printf("[HTTP] payload length: %d\n", payload.length());
+        if (payload != "") {
+          Serial.println("//////////////");
+          Serial.println(payload);
+          Serial.println("//////////////");
+          http.end();
+          return payload;
+        }
+        Serial.println("[HTTP] empty payload; retrying");
+      }
+    } else {
+      Serial.printf("[HTTP] POST... failed, error: %s (attempt=%d/%d)\n", http.errorToString(httpCode).c_str(), attempt, CHATGPT_HTTP_MAX_ATTEMPTS);
+    }
+    http.end();
+  }
   return payload;
 }
 
@@ -290,7 +328,7 @@ String ChatGPT::execChatGpt(String json_string, String& calledFunc) {
   avatar.setExpression(Expression::Doubt);
   avatar.setSpeechFont(&fonts::efontJA_16);
   avatar.setSpeechText("考え中…");
-  String ret = https_post_json("https://api.openai.com/v1/chat/completions", json_string.c_str(), root_ca_openai);
+  String ret = https_post_json(CHATGPT_API_URL, json_string.c_str(), root_ca_openai);
   avatar.setExpression(Expression::Neutral);
   avatar.setSpeechText("");
   Serial.println(ret);
@@ -299,6 +337,24 @@ String ChatGPT::execChatGpt(String json_string, String& calledFunc) {
       response = "エラーです";
       delay(1000);
       avatar.setSpeechText("");
       avatar.setExpression(Expression::Neutral);
     }else{
+      const char* simple_reply = doc["replyText"];
+      if(simple_reply != 0){
+        Serial.println(simple_reply);
+        response = String(simple_reply);
+        std::replace(response.begin(),response.end(),'\n',' ');
+        calledFunc = String("");
+        return response;
+      }
+
+      const char* error_message = doc["error"]["message"];
+      if(error_message != 0){
+        Serial.println(error_message);
+        avatar.setExpression(Expression::Sad);
+        avatar.setSpeechText("エラーです");
+        response = String(error_message);
+        delay(1000);
+        avatar.setSpeechText("");
+        avatar.setExpression(Expression::Neutral);
+        return response;
+      }
 
      const char* data = doc["choices"][0]["message"]["content"];
diff --git a/firmware/src/driver/Audio.cpp b/firmware/src/driver/Audio.cpp
index 79a248a..bbcaa73 100644
--- a/firmware/src/driver/Audio.cpp
+++ b/firmware/src/driver/Audio.cpp
@@ -1,5 +1,6 @@
 #include <M5Unified.h>
 #include "Audio.h"
+#include "driver/PlayMP3.h"
 
 Audio::Audio() {
   wavData = (typeof(wavData))heap_caps_malloc(record_size * sizeof(int16_t), MALLOC_CAP_SPIRAM | MALLOC_CAP_8BIT);
@@ -61,6 +62,11 @@ void Audio::CreateWavHeader(byte* header, int waveDataSize){
 
 void Audio::Record() {
   CreateWavHeader(paddedHeader, wavDataSize);
+  // #region agent log
+  if (!resumeMicIfPending("Audio::Record")) {
+    Serial.println("[DBG][H10] resumeMicIfPending failed in Audio::Record");
+  }
+  // #endregion
   M5.Mic.begin();
   int rec_record_idx;
   for (rec_record_idx = 0; rec_record_idx < record_number; rec_record_idx++) {
diff --git a/firmware/src/driver/PlayMP3.cpp b/firmware/src/driver/PlayMP3.cpp
index 73ffaa9..328ebb0 100644
--- a/firmware/src/driver/PlayMP3.cpp
+++ b/firmware/src/driver/PlayMP3.cpp
@@ -11,6 +11,7 @@
 #include "AudioOutputM5Speaker.h"
 #include "PlayMP3.h"
 #include "Avatar.h"
+#include "share/Mutex.h"
 
 using namespace m5avatar;
 
@@ -23,6 +24,7 @@ uint8_t m5spk_virtual_channel = 0;
 
 AudioOutputM5Speaker out(&M5.Speaker, m5spk_virtual_channel);
 AudioGeneratorMP3 *mp3;
+volatile bool g_mic_resume_pending = false;
 
 int preallocateBufferSize = 30*1024;
 uint8_t *preallocateBuffer;
@@ -45,24 +47,84 @@ void mp3_init(void)
     audioLogger = &Serial;
 }
 
+bool resumeMicIfPending(const char *caller) {
+  if (!g_mic_resume_pending) {
+    return true;
+  }
+  enterMutexAudio();
+  // #region agent log
+  Serial.printf("[DBG][H10] resumeMicIfPending caller=%s start\n", caller);
+  // #endregion
+  bool micBeginOk = M5.Mic.begin();
+  if (micBeginOk) {
+    g_mic_resume_pending = false;
+  }
+  // #region agent log
+  Serial.printf("[DBG][H10] resumeMicIfPending caller=%s done ok=%d pending=%d\n", caller, micBeginOk ? 1 : 0, g_mic_resume_pending ? 1 : 0);
+  // #endregion
+  exitMutexAudio();
+  return micBeginOk;
+}
+
 void playMP3(AudioFileSourceBuffer *buff){
+  // #region agent log
+  Serial.printf("[DBG][H4] playMP3 enter buff=%p\n", (void*)buff);
+  if (mutexAudio != nullptr) {
+    Serial.printf("[DBG][H4] mutexAudio count before=%u\n", (unsigned)uxSemaphoreGetCount(mutexAudio));
+  }
+  // #endregion
 
+  // #region agent log
+  Serial.println("[DBG][H4] mic.end start");
+  // #endregion
   M5.Mic.end();
-  M5.Speaker.begin();
-
-  mp3->begin(buff, &out);
+  // #region agent log
+  Serial.println("[DBG][H4] mic.end done");
+  // #endregion
+  // #region agent log
+  Serial.println("[DBG][H4] speaker.begin start");
+  // #endregion
+  bool speakerBeginOk = M5.Speaker.begin();
+  // #region agent log
+  Serial.printf("[DBG][H4] speaker.begin done ok=%d\n", speakerBeginOk ? 1 : 0);
+  // #endregion
+
+  bool mp3BeginOk = mp3->begin(buff, &out);
+  // #region agent log
+  Serial.printf("[DBG][H2] mp3->begin result=%d\n", mp3BeginOk ? 1 : 0);
+  // #endregion
   Serial.println("mp3 start");
 
+  int loopCount = 0;
   while(mp3->isRunning()) {
+    loopCount++;
     if (!mp3->loop()) {
+      // #region agent log
+      Serial.printf("[DBG][H2] mp3->loop false loopCount=%d\n", loopCount);
+      // #endregion
       mp3->stop();
       Serial.println("mp3 stop");
     }
     delay(1);
   }
+  // #region agent log
+  Serial.printf("[DBG][H2] mp3 loop end loopCount=%d\n", loopCount);
+  // #endregion
 
+  // #region agent log
+  Serial.println("[DBG][H4] speaker.end start");
+  // #endregion
   M5.Speaker.end();
-  M5.Mic.begin();
+  // #region agent log
+  Serial.println("[DBG][H4] speaker.end done");
+  // #endregion
+  g_mic_resume_pending = true;
+  // #region agent log
+  Serial.println("[DBG][H10] mic resume deferred until next recorder entry");
+  if (mutexAudio != nullptr) {
+    Serial.printf("[DBG][H4] mutexAudio count after=%u\n", (unsigned)uxSemaphoreGetCount(mutexAudio));
+  }
+  // #endregion
 
 }
 
diff --git a/firmware/src/driver/PlayMP3.h b/firmware/src/driver/PlayMP3.h
index b2e28a2..0f926d1 100644
--- a/firmware/src/driver/PlayMP3.h
+++ b/firmware/src/driver/PlayMP3.h
@@ -17,10 +17,12 @@ extern AudioFileSourceBuffer *buff;
 extern AudioFileSourceHTTPSStream *file;
 extern int preallocateBufferSize;
 extern uint8_t *preallocateBuffer;
+extern volatile bool g_mic_resume_pending;
 
 extern void mp3_init(void);
 extern void playMP3(AudioFileSourceBuffer *buff);
 extern bool playMP3SPIFFS(const char *filename);
 extern bool playMP3SD(const char *filename);
+extern bool resumeMicIfPending(const char *caller);
 
 #endif
\ No newline at end of file
diff --git a/firmware/src/driver/WakeWord.cpp b/firmware/src/driver/WakeWord.cpp
index fd158cd..4376ec7 100644
--- a/firmware/src/driver/WakeWord.cpp
+++ b/firmware/src/driver/WakeWord.cpp
@@ -8,6 +8,8 @@
 
 #include "simplevox.h"
 #include "WakeWord.h"
+#include "driver/PlayMP3.h"
+#include "share/Mutex.h"
 
 
 //const String file_name[REGISTER_MAX] = {"/wakeword0.bin","/wakeword1.bin","/wakeword2.bin","/wakeword3.bin","/wakeword4.bin",
@@ -58,6 +60,18 @@ int16_t* rxMic()
   static int rxIndex = 0;
   const int frameLength = vadEngine.config().frame_length();
 
+  // #region agent log
+  if (!resumeMicIfPending("WakeWord::rxMic")) {
+    Serial.println("[DBG][H10] resumeMicIfPending failed in WakeWord::rxMic");
+  }
+  // #endregion
+
+  // #region agent log
+  if (mutexAudio != nullptr && uxSemaphoreGetCount(mutexAudio) == 0) {
+    Serial.println("[DBG][H9] rxMic called while mutexAudio is locked");
+  }
+  // #endregion
+
   if (!M5.Mic.record(&rxBuffer[frameLength * rxIndex], frameLength, kSampleRate))
   {
     return nullptr;